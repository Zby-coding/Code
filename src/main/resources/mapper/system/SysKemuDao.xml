<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.jeefast.system.dao.SysKemuDao">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="cn.jeefast.system.entity.SysKemu">
		<id column="id" property="id" />
		<result column="name" property="name" />
		<result column="createtime" property="createtime" />
		<result column="updatetime" property="updatetime" />
		<result column="createuser" property="createuser" />
		<result column="updateuser" property="updateuser" />
	</resultMap>
	<delete id="deleteBatch">
		delete from sys_kemu where id in
		<foreach item="kemuIds" collection="array" open="(" separator="," close=")">
			#{kemuIds}
		</foreach>
	</delete>
	<select id="queryPageList" resultType="cn.jeefast.system.entity.SysKemu">
		select r.*  from sys_kemu r
		<where>
			<if test="name != null and name.trim() != ''">
				and r.`name` like concat('%',#{name},'%')
			</if>
		</where>
		order by r.updatetime desc
	</select>

</mapper>
