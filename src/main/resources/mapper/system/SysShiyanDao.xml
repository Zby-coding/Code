<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.jeefast.system.dao.SysShiyanDao">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="cn.jeefast.system.entity.SysShiyan">
		<id column="id" property="id" />
		<result column="name" property="name" />
		<result column="content" property="content" />
		<result column="type" property="type" />
		<result column="teacher" property="teacher" />
		<result column="createtime" property="createtime" />
		<result column="updatetime" property="updatetime" />
		<result column="createuser" property="createuser" />
		<result column="updateuser" property="updateuser" />
	</resultMap>
	<delete id="deleteBatch">
		delete from sys_shiyan where id in
		<foreach item="shiyanIds" collection="array" open="(" separator="," close=")">
			#{shiyanIds}
		</foreach>
	</delete>
	<select id="queryPageList" resultType="cn.jeefast.system.entity.SysShiyan">
		select r.*,sk.name typename  from sys_shiyan r
		left join sys_kemu sk
		on r.type = sk.id
		<where>
			<if test="name != null and name.trim() != ''">
				and r.`name` like concat('%',#{name},'%')
			</if>
		</where>
		order by r.updatetime desc
	</select>

</mapper>
